<div>
    @livewire('product.detail-product.detail', ['slug' => $slug])
    @livewire('product.detail-product.similar-product', ['slug' => $slug])
    @livewire('product.detail-product.feed-back-product', ['slug' => $slug])
</div>
